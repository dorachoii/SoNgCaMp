using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class EJDragNote_test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        //pad에 trigger되고 판정범위 내에서 눌리면 정 위치로 옮겨오기
        //input 값에 따라, horizon값에 따라 Drag 되기
        //그 drag값이 trigger되면 점수 체크


        //직선 long Note의 경우: 첫노드~마지막 노드 잇는 것을 lineRenderer로만 했는데 이거 세로 길이 어떻게 늘릴지 생각

        
    }
}
